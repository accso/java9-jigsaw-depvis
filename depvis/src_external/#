# Java 9 Jigsaw modules example suite
# see https://github.com/accso/java9-jigsaw-examples
# ------------------------------------------------------------------------------
# Example for --add-reads and --add-exports for javac compiler and java launcher
# ------------------------------------------------------------------------------
# Modules in this example: modb, modc, modmain
# 
# modmain has a Main class which is started via run.sh
# ------------------------------------------------------------------------------
#
# All of the module-info.java are empty
#
# All is done in compile.sh and run.sh
#
# a) modb needs a read relationship to modc
# b) modc needs to export its package pkgc to modb
# c) modmain needs a read relationship to modb
# d) modb needs to exports it package pkgb to modmain
# ------------------------------------------------------------------------------
 